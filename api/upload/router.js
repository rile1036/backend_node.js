/*const express = require('express');
const path = require('path');
const fs = require('fs');
const { create } = require("./upload_router");

var router = require('express').Router();
var keyword;

router.get('/:content_id',  create);

module.exports = router;*/